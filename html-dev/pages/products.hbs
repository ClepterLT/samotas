{{>base/head
  bodyClasses=bodyClasses
}}

    {{>modules/header
        extraHeaderClasses=''
    }}

    <main>

        <section class="categories">
            <div class="row u-flex u-justify-end">
                <div class="dropdown">
                    <div class="dropdown__button">Pasirinkite kateogriją &#8628;</div>
                    <div class="dropdown__list">
                        {{#each categories}}
                        <a href="{{href}}" class="dropdown__item">{{title}}</a>
                        {{/each}}
                    </div>
                </div>
            </div>
        </section>

        <section class="products">
            <div class="row u-flex-center u-flex-wrap">
                {{#each products}}
                        {{>modules/img-with-box-small}}
                {{/each}}
            </div>
        </section>

    {{>modules/cta}}

    </main>

{{>base/footer
  bodyClasses=bodyClasses
}}